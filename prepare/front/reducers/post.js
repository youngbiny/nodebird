export const intialState = {  
  mainPosts:[{
    id:1,
    User: {
      id:1,
      nickname:'제로초',
    }
  },
  content:
  ]
}

const reducer = (state = intialState, aciton) => {
  switch(aciton.type){
      default :
        return  state;
  }
}

export default reducer;